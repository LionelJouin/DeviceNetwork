---
apiVersion: resource.k8s.io/v1
kind: ResourceClaimTemplate
metadata:
  name: my-network
spec:
  spec:
    devices:
      requests:
      - name: my-network
        exactly:
          deviceClassName: devicenetwork
          allocationMode: ExactCount
          count: 1
          selectors:
          - cel:
              expression: device.attributes["multinetwork.networking.k8s.io"].podNetwork == "my-network"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo
  labels:
    app: demo
  annotations:
    required-plugins.noderesource.dev/pod: '["devicenetwork"]'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      containers:
      - name: alpine
        image: alpine:latest
        imagePullPolicy: IfNotPresent
        command:
        - sleep
        - infinity
      resourceClaims:
      - name: my-network
        resourceClaimTemplateName: my-network